# Chatwoot 自托管部署方案（aaPanel 环境）

> 目标：在你的 VPS（已装 aaPanel + Docker）上部署 Chatwoot 开源客服系统，
> 配合官网 Widget + 独立 `/chat` 页面 + iOS/Android 客服 App，实现手机统一收回复客户消息。

---

## 一、架构

```
客户浏览器/手机
      │
      ▼
https://chat.你的域名.com   ← aaPanel Nginx + SSL（Let's Encrypt）
      │
      ▼ 反向代理（WebSocket）
127.0.0.1:3000
      │
Docker Compose（/opt/chatwoot）
      ├── chatwoot-rails    ── Web / API / Widget
      ├── chatwoot-sidekiq  ── 后台任务（邮件、Webhook 等）
      ├── chatwoot-postgres ── PostgreSQL 16（pgvector）
      └── chatwoot-redis    ── Redis（会话/队列）

推送链路：客户发消息 → Rails → Push Relay → Apple/Google 推送 → 你的 iPhone/Android 客服 App 🔔
```

## 二、前置条件

| 项目 | 要求 |
|---|---|
| VPS | 官方建议 ≥ 2 核 / 4GB RAM / 30GB SSD（仅有你一个客服时 2C4G 够用） |
| 系统 | Ubuntu 22.04/24.04、Debian 12、CentOS/Alma 均支持 |
| 域名 | 一个解析到 VPS 的域名（推荐 `chat.xxx.com`），HTTPS 必须 |
| aaPanel | 用于 Nginx + SSL + 站点管理 |
| Docker | 已安装（aaPanel 自带或系统安装均可） |

## 三、部署步骤（SSH 执行）

### 1. 上传部署包到 VPS

本机执行（或直接复制整个 `chatwoot-deploy` 目录）：

```bash
scp -r chatwoot-deploy root@你的IP:/root/chatwoot-deploy
```

### 2. 登录 VPS，配置 .env

```bash
ssh root@你的IP
cd /root/chatwoot-deploy
cp .env.example .env
nano .env
```

必须修改一行：

```ini
FRONTEND_URL=https://chat.你的域名.com
```

其他密钥项（`SECRET_KEY_BASE` / `POSTGRES_PASSWORD` / `REDIS_PASSWORD`）留空即可，脚本会自动生成。
可选：配置 `SMTP_*` 用于发信；`ENABLE_ACCOUNT_SIGNUP` 注册好管理员后改为 `false`。

### 3. 一键部署

```bash
sudo bash deploy.sh
```

脚本自动完成：装/检测 Docker → 生成随机密钥 → `docker compose -f docker-compose.production.yaml up -d` → 初始化数据库（`rails db:chatwoot_prepare`）。

检查：

```bash
cd /root/chatwoot-deploy && docker compose -f docker-compose.production.yaml ps
curl -s http://127.0.0.1:3000 | head
```

## 四、aaPanel 配置 HTTPS 反向代理

1. **DNS**：在域名服务商把 `chat.你的域名.com` 解析到 VPS IP（A 记录）。
2. **添加站点**：aaPanel → 网站 → 添加站点 → 域名填 `chat.你的域名.com`（纯静态即可）。
3. **申请 SSL**：网站 → SSL → Let's Encrypt → 申请（自动续期）。
4. **反向代理**：网站 → 反向代理 → 添加：
   - 代理名称：`chatwoot`
   - 目标 URL：`http://127.0.0.1:3000`
   - 发送域名：`$host`
   - **启用 WebSocket 支持 ✔**（实时聊天必需）
5. **上传限制**：站点配置里加 `client_max_body_size 20m;`（客户传图/文件）。
6. 不开反代、直接用端口访问也可以：`http://IP:3000`（仅测试用，不建议正式使用）。

## 五、初始化 Chatwoot

1. 浏览器打开 `https://chat.你的域名.com`，注册管理员账号（官方默认注册路径 `/app/auth/signup`）。**建议注册后立即在 .env 里把 `ENABLE_ACCOUNT_SIGNUP=false` 并重启。**
2. 创建收件箱：**Settings → Inboxes → Add Inbox → Website**
   - Inbox name：`网站客服`
   - Website domain：你的官网域名
   - 得到一段 Widget JS（含 `websiteToken`），复制备用。
3. 把 Widget JS 放到你的网站 `<head>` 或 footer，客户访问右下角即出现 💬。

## 六、iPhone / Android 客服 App（推送）

1. 手机安装官方 **Chatwoot** App（App Store / Google Play）。
2. 打开 App → 选择 "Add server" → 输入 `https://chat.你的域名.com` → 用管理员账号登录。
3. 推送：本方案已启用内置 Push Relay（`ENABLE_PUSH_RELAY_SERVER=true`），客户来消息会直接推到手机，无需自己配置 APNs/FCM。

## 七、独立聊天链接 `/chat`（推荐加）

客户不用找网页右下角，直接发链接就能聊。把下面代码保存为 `chat.html` 放到你的网站任意位置：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>联系客服</title>
  <style>
    body { font-family: sans-serif; max-width: 640px; margin: 0 auto; padding: 24px; }
    h1 { font-size: 22px; }
  </style>
</head>
<body>
  <h1>👋 你好，有什么可以帮你？</h1>
  <p>直接留言即可，我们会尽快回复。</p>

  <script>
    window.chatwootSettings = { position: 'right', type: 'expanded_bubble' };
    (function (d, t) {
      var BASE_URL = "https://chat.你的域名.com";      // ← 改成你的 Chatwoot 地址
      var g = d.createElement(t), s = d.getElementsByTagName(t)[0];
      g.src = BASE_URL + "/packs/js/sdk.js";
      g.defer = true; g.async = true;
      s.parentNode.insertBefore(g, s);
      g.onload = function () {
        window.chatwootSDK.run({
          websiteToken: '在这里填你的 websiteToken',    // ← 从 Inbox 设置里复制
          baseUrl: BASE_URL
        });
        window.$chatwoot.setExpanded(true);            // 自动展开聊天窗口
      }
    })(document, "script");
  </script>
</body>
</html>
```

之后对外发这样的链接即可：`你的域名.com/chat?from=wechat`（来源渠道可以用 Chatwoot 自定义属性记录）。

## 八、备份与恢复

**备份（每天）**：在 `/root/chatwoot-deploy` 下执行

```bash
sudo bash backup.sh
```

生成 `backups/db_日期.sql.gz`（PostgreSQL 全库）+ `backups/storage_日期.tar.gz`（客户上传文件），自动保留 7 天。
建议：aapanel 面板的「计划任务」每天执行一次，并把 `backups/` 目录再同步到其他存储（如对象存储/另一台机器）。

**恢复**：

```bash
cd /root/chatwoot-deploy
# 恢复数据库（先停 rails/sidekiq 或直接执行均可）
gunzip -c backups/db_日期.sql.gz | docker compose -f docker-compose.production.yaml exec -T postgres psql -U postgres -d chatwoot_production
# 恢复上传文件
tar -xzf backups/storage_日期.tar.gz -C /tmp/storage_restore
docker cp /tmp/storage_restore/. $(docker compose -f docker-compose.production.yaml ps -q rails):/app/storage/
```

## 九、升级 Chatwoot

```bash
cd /root/chatwoot-deploy
sudo bash backup.sh                        # 1. 先备份
docker compose -f docker-compose.production.yaml pull                        # 2. 拉新版镜像
docker compose -f docker-compose.production.yaml up -d                       # 3. 重建容器
docker compose -f docker-compose.production.yaml run --rm rails bundle exec rails db:migrate    # 4. 升级数据库
docker compose -f docker-compose.production.yaml restart rails sidekiq       # 5. 重启
```

## 十、安全与日常

- **防火墙**：只对外开放 80/443 与 SSH；3000/5432/6379 已绑定 127.0.0.1，不要对公网开放。
- aaPanel 自带 Fail2ban/防火墙插件可用；SSH 建议改用密钥登录。
- `backups/` 含数据库密码明文？不含——`.env` 里的密钥才需要保密，不要把这个目录传到公开仓库。
- 磁盘监控：图片上传多时关注磁盘使用（`df -h`）。

## 十一、常见问题

| 问题 | 处理 |
|---|---|
| 2核4G 够吗 | 一个客服够用；客户同时在线多再升 4C8G |
| 不想用域名 | 先 `http://IP:3000` 用，正式使用再配域名（推送/微信等渠道需要 HTTPS） |
| 收不到手机推送 | 确认 `.env` 有 `ENABLE_PUSH_RELAY_SERVER=true`，并重启容器；App 登录的是 `https://chat.你的域名.com` |
| 聊天不实时 | 反代必须开 WebSocket（见第四节第 4 步） |
| 客户上传不了大图 | Nginx `client_max_body_size 20m;` |
| 中文界面 | `.env` 已设 `DEFAULT_LOCALE=zh_CN`，注册新账号后生效 |

---

*配套文件：`docker-compose.production.yaml`、`.env.example`、`deploy.sh`、`backup.sh`、`nginx-chatwoot.conf.example`*
